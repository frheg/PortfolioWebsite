// Order and identity of the sections stacked on the single scrolling page.
// These are internal keys, not routes — must match spaceConfig.camera.pageStops
// in src/three/spaceConfig.js, whose orbit angles are keyed by these same
// identifiers, in this same order.
export const chapterOrder = ['/', '/projects', '/journey', '/contact', '/chat']
